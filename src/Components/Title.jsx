

const Title = ({title}) => {
    return (
        <div>
            <h2 className="text-5xl font-semibold my-8 text-center uppercase text-pink-500 bg-slate-200 py-5 my-10 rounded-full shadow">{title}</h2>
        </div>
    );
};

export default Title;